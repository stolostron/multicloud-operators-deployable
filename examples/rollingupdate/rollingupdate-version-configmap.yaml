apiVersion: multicloud-apps.io/v1
kind: Deployable
metadata:
  annotations:
    multicloud-apps.io/is-local-deployable: "false"
  name: version-configmap
  namespace: default
spec:
  template:
    apiVersion: v1
    kind: ConfigMap
    metadata:
      namespace: default
    data:
      purpose: rolling update
